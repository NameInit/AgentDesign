*   КЛАССИФИКАЦИЯ: Схема/алгоритм, проекция, матрица преобразований.
*   СУТЬ И КОНТЕКСТ: Схема алгоритма построения матрицы преобразования. Контекст: математическое вычисление, инженерная геометрия, компьютерная графика.
*   ДЕТАЛИЗАЦИЯ:

    *   Матрица преобразования — 3x3 матрица.
    *   Базовый вектор — вектор, который является первым столбцом матрицы преобразования.
    *   Вектор нормали — вектор, который является вторым столбцом матрицы преобразования.
    *   Вектор направления — вектор, который является третьим столбцом матрицы преобразования.
    *   Вектор нормали — вектор, который является вторым столбцом матрицы преобразования.
    *   Вектор направления — вектор, который является третьим столбцом матрицы преобразования.
    *   Вектор нормали — вектор, который является вторым столбцом матрицы преобразования.
    *   Вектор направления — вектор, который является третьим столбцом матрицы преобразования.
    *   Вектор нормали — вектор, который является вторым столбцом матрицы преобразования.
    *   Вектор направления — вектор, который является третьим столбцом матрицы преобразования.
    *   Вектор нормали — вектор, который является вторым столбцом матрицы преобразования.
    *   Вектор направления — вектор, который является третьим столбцом матрицы преобразования.
    *   Вектор нормали — вектор, который является вторым столбцом матрицы преобразования.
    *   Вектор направления — вектор, который является третьим столбцом матрицы преобразования.
    *   Вектор нормали — вектор, который является вторым столбцом матрицы преобразования.
    *   Вектор направления — вектор, который является третьим столбцом матрицы преобразования.
    *   Вектор нормали — вектор, который является вторым столбцом матрицы преобразования.
    *   Вектор направления — вектор, который является третьим столбцом матрицы преобразования.
    *   Вектор нормали — вектор, который является вторым столбцом матрицы преобразования.
    *   Вектор направления — вектор, который является третьим столбцом матрицы преобразования.
    *   Вектор нормали — вектор, который является вторым столбцом матрицы преобразования.
    *   Вектор направления — вектор, который является третьим столбцом матрицы преобразования.
    *   Вектор нормали — вектор, который является вторым столбцом матрицы преобразования.
    *   Вектор направления